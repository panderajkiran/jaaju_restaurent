# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
.vercel
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets & credentials — never commit (see CLAUDE.md)
.env
.env.*
!.env.example
credentials.json
token.json

# Temporary / scratch workspace
.tmp/

# Loose source-image drops in the project root.
# The originals are already copied into src/assets — these root copies are
# redundant and only bloat the repo. (Anchored to root so src/assets is kept.)
/*.png
/*.jpg
/*.jpeg
/*.webp
/*.avif
/*.gif
